import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.eata.invernaderoapp',
  appName: 'InvernaderoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
